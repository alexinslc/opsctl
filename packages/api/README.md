# API Package

This is the API package built with FastAPI.

## Installation

```bash
uv pip install -e packages/api
```

## Usage

```bash
uvicorn opsctl_api.main:app --reload
```

The API will be available at http://localhost:8000
